name: 'DPI Node'

services:
  db:
    image: mysql:8.4.7
    container_name: mysql8_container_frontiers
    hostname: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -u twin -ptwin --silent || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 30s
    ports:
      - "3333:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./init:/docker-entrypoint-initdb.d

  dpi-node-provider:
    image: twinfoundation/twin-node:0.0.3-next.50
    container_name: dpi_node_provider
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "3010:3000"
    env_file: 
      - ./common/.env
      - ./.env.provider
    volumes: 
      - node_provider_data:/var/lib/twin
      - ./common/.env.bootstrap:/app/.env.bootstrap
      - ./apps/dataspace-example-app:/app/node_modules/@twin-community.org/dataspace-example-app
      - ./dataset:/app/data

  dpi-node-consumer:
    image: twinfoundation/twin-node:0.0.3-next.50
    container_name: dpi_node_consumer
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "3010:3000"
    env_file: 
      - ./common/.env
      - ./.env.consumer
    volumes: 
      - node_consumer_data:/var/lib/twin
      - ./common/.env.bootstrap:/app/.env.bootstrap
      - ./apps/consumer-client:/app/node_modules/@twin-community.org/consumer-client
      - ./dataset:/app/data
      
volumes:
  mysql_data:
  node_consumer_data:
  node_provider_data:
